# 文件组织迁移指南

## 📋 概述
本文档说明如何完成项目文件重组后的场景文件更新步骤。

---

## ✅ 已完成的步骤

1. ✅ 创建了新的文件夹结构 (`scripts/`)
2. ✅ 复制了所有脚本到新位置
3. ✅ 更新了 `project.godot` 中的 autoload 配置
4. ✅ 旧文件保留在原位置（未删除）

---

## 🔧 自动化迁移步骤（推荐）

### 步骤1: 准备工作

1. **确保项目已提交到 git**（非常重要！）
   ```bash
   git add .
   git commit -m "迁移前的备份"
   ```

2. **关闭 Godot 编辑器**

### 步骤2: 批量更新场景文件路径

使用 `sed` 批量替换 `.tscn` 文件中的脚本路径：

```bash
# 更新 Scripts/ 路径到 scripts/
find scenes -name "*.tscn" -exec sed -i 's|res://Scripts/|res://scripts/|g' {} +

# 更新 player/ 路径到 scripts/actors/player/
find scenes -name "*.tscn" -exec sed -i 's|res://player/scripts/|res://scripts/actors/player/|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://player/states/|res://scripts/actors/player/states/|g' {} +

# 更新根目录脚本到 scripts/
find scenes -name "*.tscn" -exec sed -i 's|res://Global\.gd"|res://scripts/autoload/global_data.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://slime_green\.gd"|res://scripts/actors/enemies/slime_green.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://coco_adventure\.gd"|res://scripts/levels/coco_adventure.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://dungeon\.gd"|res://scripts/levels/dungeon.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://door\.gd"|res://scripts/items/door.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://box\.gd"|res://scripts/items/box.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://treasure_box\.gd"|res://scripts/items/treasure_box.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://flying_pig\.gd"|res://scripts/actors/npc/flying_pig.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://fall_box\.gd"|res://scripts/traps/falling_box.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://fall_stone\.gd"|res://scripts/traps/falling_stone.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://kill_zone\.gd"|res://scripts/traps/kill_zone.gd"|g' {} +
find scenes -name "*.tscn" -exec sed -i 's|res://area_2d\.gd"|res://scripts/components/interaction_area.gd"|g' {} +
```

### 步骤3: 更新 project.godot

手动编辑 `project.godot`，更新所有 autoload 路径：

```ini
[autoload]
GlobalData="*res://scripts/autoload/global_data.gd"
SaveLoad="*res://scripts/autoload/save_system.gd"
PlayerHealth="*res://scripts/actors/player/player_health.gd"
LevelManager="*res://scripts/managers/level_manager.gd"
ShopUI="*res://scenes/shop_ui.tscn"
TradeItemList="*res://scripts/autoload/trade_item_list.gd"
```

### 步骤4: 提交更改

```bash
git add -A
git commit -m "更新所有场景文件的脚本路径引用"
```

---

## ⚠️ 迁移过程中遇到的实际问题

### 问题1: Windows 文件系统不区分大小写

**症状**：
- 删除 `Scripts/` 文件夹时，`scripts/` 文件夹也被删除
- 文件丢失错误：`File not found: res://scripts/autoload/global_data.gd`

**原因**：
Windows 系统将 `Scripts/` 和 `scripts/` 视为同一个文件夹。执行 `rm -rf Scripts/` 会同时删除两个文件夹。

**解决方案**：
1. **从 git 恢复文件**：
   ```bash
   git checkout <上一个提交hash> -- Scripts/
   ```

2. **重命名文件夹**：
   ```bash
   mv Scripts scripts
   ```

**预防措施**：
- ✅ 统一使用小写 `scripts/` 文件夹名
- ✅ 迁移前确保已提交到 git
- ✅ 不要同时存在 `Scripts/` 和 `scripts/` 文件夹

---

### 问题2: Parser Error - Class 隐藏全局脚本类

**错误信息**：
```
Parser Error: Class "SaveSystem" hides a global script class.
Parser Error: Class "Slime" hides a global script class.
Parser Error: Class "Door" hides a global script class.
```

**原因**：
- Autoload 单例脚本使用了 `class_name`
- 新旧位置的脚本都定义了相同的 `class_name`
- Godot 4.x 中 autoload 脚本已经是全局单例，不需要 `class_name`

**解决方案**：

1. **移除 autoload 脚本的 class_name**：
   ```gdscript
   # 错误示范
   extends Node
   class_name SaveSystem  # ❌ 会与全局单例冲突

   # 正确示范
   extends Node
   # autoload 脚本不需要 class_name，会与全局单例冲突
   ```

2. **删除旧位置的重复文件**：
   ```bash
   # 确保新位置的脚本已更新后，删除旧文件
   rm -rf Scripts/
   rm -rf player/
   ```

**影响的文件**：
- `scripts/autoload/save_system.gd` - 移除 `class_name SaveSystem`
- `scripts/actors/enemies/slime_green.gd` - 移除 `class_name Slime`
- `scripts/items/door.gd` - 移除 `class_name Door`

---

### 问题3: 场景文件脚本路径未更新

**错误信息**：
```
E 0:00:02:870 level_manager.gd:28 @ goto_next_room():
Attempt to open script 'res://coco_adventure.gd' resulted in error 'File not found'
```

**原因**：
场景文件（.tscn）中的 `ext_resource` 仍指向旧路径

**解决方案**：
使用批量 `sed` 替换更新所有 .tscn 文件（见步骤2）

**验证**：
```bash
# 检查是否还有旧路径引用
grep -r "res://Scripts/" scenes/
grep -r "res://player/" scenes/
```

---

### 问题4: UID 不匹配警告

**警告信息**：
```
W 0:00:02:696 level_manager.gd:28 @ goto_next_room():
res://scenes/enemies/slime_green.tscn:4 - ext_resource, invalid UID: uid://bs07w5jexn0lw
- using text path instead
```

**原因**：
脚本文件移动后，场景文件中的 UID 引用未更新

**影响**：
- 游戏仍可正常运行（Godot 会回退到使用文本路径）
- 控制台会显示警告信息

**解决方案**：

1. **手动更新单个场景**（推荐）：
   - 在编辑器中打开场景
   - 重新选择脚本（即使路径相同也要重新指定）
   - 保存场景

2. **批量更新 UID**：
   创建工具脚本 `scripts/utils/update_scene_uids.gd`：
   ```gdscript
   @tool
   extends EditorScript

   func _run() -> void:
       # 读取脚本的 .uid 文件
       # 更新场景文件中的 UID 引用
   ```

   在编辑器中运行：项目 → 工具 → 运行脚本

---

## 🗑️ 清理旧文件（确认测试通过后）

**仅在确认所有功能正常后执行**：

```bash
# ⚠️ 警告：Windows 系统会同时删除 Scripts/ 和 scripts/
# 确保新位置的 scripts/ 文件夹已备份到其他位置！

# 安全做法：
git add -A
git commit -m "清理前的最终备份"

# 逐个删除旧文件
rm -f Global.gd slime_green.gd coco_adventure.gd
rm -f door.gd box.gd treasure_box.gd flying_pig.gd
rm -f fall_box.gd fall_stone.gd kill_zone.gd area_2d.gd dungeon.gd

# 删除旧文件夹（小心！）
rm -rf Scripts/
rm -rf player/
```

---

## 📋 路径映射表

### 完整的路径映射

| 旧路径 | 新路径 |
|--------|--------|
| `Global.gd` | `scripts/autoload/global_data.gd` |
| `Scripts/save_system.gd` | `scripts/autoload/save_system.gd` |
| `Scripts/autoload/trade_item_list.gd` | `scripts/autoload/trade_item_list.gd` |
| `Scripts/managers/level_manager.gd` | `scripts/managers/level_manager.gd` |
| | |
| `player/scripts/player.gd` | `scripts/actors/player/player.gd` |
| `player/scripts/player_health.gd` | `scripts/actors/player/player_health.gd` |
| `player/states/*.gd` | `scripts/actors/player/states/*.gd` |
| | |
| `Scripts/knight.gd` | `scripts/actors/player/knight.gd` |
| `Scripts/enemies/bat.gd` | `scripts/actors/enemies/bat.gd` |
| `Scripts/enemies/bat_spawner.gd` | `scripts/actors/enemies/bat_spawner.gd` |
| `Scripts/enemies/slime_purple_boss.gd` | `scripts/actors/enemies/slime_boss.gd` |
| `slime_green.gd` | `scripts/actors/enemies/slime_green.gd` |
| | |
| `Scripts/shop_keeper.gd` | `scripts/actors/npc/shop_keeper.gd` |
| `scenes/NPC/npc.gd` | `scripts/actors/npc/npc.gd` |
| `flying_pig.gd` | `scripts/actors/npc/flying_pig.gd` |
| | |
| `Scripts/coin.gd` | `scripts/items/coin.gd` |
| `Scripts/health_pot.gd` | `scripts/items/health_potion.gd` |
| `Scripts/sword.gd` | `scripts/items/sword.gd` |
| `door.gd` | `scripts/items/door.gd` |
| `box.gd` | `scripts/items/box.gd` |
| `treasure_box.gd` | `scripts/items/treasure_box.gd` |
| | |
| `Scripts/HUD.gd` | `scripts/ui/hud.gd` |
| `Scripts/chat_bubble.gd` | `scripts/ui/chat_bubble.gd` |
| `Scripts/shop_ui.gd` | `scripts/ui/shop_ui.gd` |
| `Scripts/start_game.gd` | `scripts/ui/start_menu.gd` |
| | |
| `Scripts/platform_a_active.gd` | `scripts/traps/active_platform.gd` |
| `fall_box.gd` | `scripts/traps/falling_box.gd` |
| `fall_stone.gd` | `scripts/traps/falling_stone.gd` |
| `kill_zone.gd` | `scripts/traps/kill_zone.gd` |
| `scenes/traps/spike.gd` | `scripts/traps/spike.gd` |
| `scenes/traps/waterfall.gd` | `scripts/traps/waterfall.gd` |
| `area_2d.gd` | `scripts/components/interaction_area.gd` |
| | |
| `Scripts/shop.gd` | `scripts/levels/shop.gd` |
| `coco_adventure.gd` | `scripts/levels/coco_adventure.gd` |
| `dungeon.gd` | `scripts/levels/dungeon.gd` |
| `Scripts/oscar_adventure.gd` | `scripts/levels/oscar_adventure.gd` |
| `scenes/boss_01_scene.gd` | `scripts/levels/boss_arena.gd` |
| | |
| `Scripts/tools/create_king_animation.gd` | `scripts/utils/create_king_animation.gd` |

---

## ⚠️ 故障排查清单

### Q1: 文件丢失错误 - File not found
**症状**: `Attempt to open script 'res://scripts/autoload/global_data.gd' resulted in error 'File not found'`
**可能原因**:
1. Windows 大小写不敏感导致误删
2. 脚本未正确复制到新位置
**解决步骤**:
```bash
# 检查文件是否存在
ls scripts/autoload/global_data.gd

# 如果文件丢失，从 git 恢复
git checkout <上一个提交hash> -- Scripts/
mv Scripts scripts
```

### Q2: Parser Error - Class 隐藏全局脚本类
**症状**: `Parser Error: Class "SaveSystem" hides a global script class.`
**原因**: Autoload 脚本使用了 `class_name`
**解决步骤**:
1. 打开报错的脚本文件
2. 删除 `class_name XXX` 行
3. 保存文件
4. 重启 Godot 编辑器

### Q3: 运行时场景无法加载
**症状**: `Attempt to open script 'res://coco_adventure.gd' resulted in error 'File not found'`
**原因**: .tscn 文件中的脚本路径未更新
**解决步骤**:
```bash
# 检查场景文件中的旧路径
grep -r "res://Scripts/" scenes/
grep -r "res://player/" scenes/
grep -r "res://coco_adventure.gd" scenes/

# 批量更新路径（见步骤2）
```

### Q4: UID 不匹配警告
**症状**: `invalid UID: uid://bs07w5jexn0lw - using text path instead`
**影响**: 游戏可运行但有警告
**解决步骤**:
1. 方法A（推荐）：在编辑器中打开场景，重新选择脚本，保存
2. 方法B（批量）：运行 `scripts/utils/update_scene_uids.gd` 工具脚本

### Q5: 动画或交互不工作
**可能原因**:
1. 脚本路径未更新导致使用旧版本脚本
2. Autoload 配置错误
**解决步骤**:
1. 检查 `project.godot` 的 `[autoload]` 配置
2. 检查场景的脚本引用路径
3. 检查 `preload()` 和 `load()` 调用中的路径

### Q6: Git 提交后发现文件丢失
**解决步骤**:
```bash
# 查看提交历史
git log --oneline

# 恢复到上一个正确状态
git checkout <上一个正确提交hash> -- Scripts/

# 或恢复单个文件
git checkout <commit> -- Scripts/autoload/global_data.gd
```

---

## ✅ 最佳实践总结

### 迁移前准备

1. **✅ 必须提交到 git**
   ```bash
   git add .
   git commit -m "迁移前的完整备份"
   ```

2. **✅ 创建新分支测试**
   ```bash
   git checkout -b file-reorganization
   ```

3. **✅ 关闭 Godot 编辑器**
   - 避免编辑器自动保存覆盖修改

### Windows 系统特殊注意事项

1. **⚠️ 文件夹命名约定**
   - 统一使用小写：`scripts/`、`assets/`、`scenes/`
   - 不要使用 `Scripts/`、`Assets/` 等大写开头
   - 避免同时存在大小写不同的同名文件夹

2. **⚠️ 删除文件夹前检查**
   ```bash
   # 错误：可能同时删除 scripts/
   rm -rf Scripts/

   # 正确：先检查是否存在冲突
   ls -l | grep -i scripts
   ```

3. **⚠️ 重命名文件夹**
   ```bash
   # 安全重命名方法
   mv Scripts scripts_temp
   mv scripts_temp scripts
   ```

### 自动化工具使用

1. **批量文本替换**
   - ✅ 使用 `find` + `sed` 批量更新 .tscn 文件
   - ✅ 提交每个批次的修改（便于回滚）
   - ❌ 不要尝试手动编辑每个场景

2. **UID 更新**
   - 创建工具脚本自动化处理
   - 或在编辑器中手动重新指定脚本

### 验证和测试

1. **检查路径更新**
   ```bash
   # 搜索是否还有旧路径
   grep -r "res://Scripts/" scenes/
   grep -r "res://player/scripts/" scenes/
   ```

2. **测试关键场景**
   - 主菜单
   - 玩家角色
   - 商店系统
   - 关卡切换
   - 敌人AI

3. **检查控制台输出**
   - 无 Parser Error
   - 无 File not found
   - 警告信息可接受（UID 警告不影响功能）

---

## 📊 本次迁移统计

- **移动的文件数量**: 40+ 脚本文件
- **更新的场景文件**: 38 个 .tscn 文件
- **遇到的主要问题**: 4 个（见上方"实际遇到的问题"）
- **Git 提交次数**: 13 次
- **迁移耗时**: ~2 小时（包含问题排查）

---

## 📞 需要帮助？

如果在迁移过程中遇到问题：

1. 检查本文档的路径映射表
2. 查看 Godot 编辑器的输出面板错误信息
3. 参考 `项目文件组织规范.md` 了解新的文件结构

---

**文档版本**: 2.0
**最后更新**: 2025-02-11
**更新内容**: 基于实际迁移经验重构，添加真实问题与解决方案
**适用项目**: Little Knight Adventure v0.5
